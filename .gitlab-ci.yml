image: alpine

pages:
  script:
  - apk --no-cache add py2-pip python-dev
  - pip install sphinx
  - apk --no-cache add make
  - sphinx-build html .
  - mv . public/
  artifacts:
    paths:
    - public
  only:
  - master


    


